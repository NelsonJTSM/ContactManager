let express,mongoose,bodyParser,dotenv,routes;_749‍.w("express",[["default",["express"],function(v){express=v}]]);_749‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_749‍.w("body-parser",[["default",["bodyParser"],function(v){bodyParser=v}]]);_749‍.w("dotenv",[["default",["dotenv"],function(v){dotenv=v}]]);_749‍.w("./src/routes/userRoutes",[["default",["routes"],function(v){routes=v}]]);





dotenv.config();

const app = express();
const PORT = process.env.PORT;

// MongoDB connection.
mongoose.Promise = global.Promise;
const MONGO_USER = process.env.MONGO_USERNAME;
const MONGO_PASS = process.env.MONGO_PASSWORD;
const URI = "mongodb+srv://" + MONGO_USER + ":" + MONGO_PASS + "@maincluster-bmnhf.mongodb.net/test?retryWrites=true&w=majority";
mongoose.connect(URI, { useNewUrlParser: true, useUnifiedTopology: true });

// BodyParser Setup:
app.use(bodyParser.urlencoded({extended: false}));
app.use(bodyParser.json());

routes(app);

app.get('/', (req, res) => {
	res.send(`Node and express server is running on port ${PORT}`);
});

app.get('/api/test', (req, res) => {
	res.send(`Node and express server is running on port ${PORT}`);
});

app.listen(PORT, () => {
	console.log(`Your server is runing on port ${PORT}`);
});